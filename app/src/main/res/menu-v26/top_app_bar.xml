<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/unmatch"
        android:icon="@drawable/ic_unmatch"
        android:title="@string/unmatch"
        android:contentDescription="@string/button_to_unmatch_users"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/swap"
        android:icon="@drawable/ic_swap"
        android:title="@string/swap"
        android:contentDescription="@string/button_to_swap_books"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/actionSearch"
        android:icon="@android:drawable/ic_menu_search"
        android:title="@string/search"
        android:iconTint="@color/black"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />
</menu>