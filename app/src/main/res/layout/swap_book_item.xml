<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    app:layout_goneMarginStart="10dp"
    app:layout_goneMarginEnd="10dp"
    android:layout_height="wrap_content"
    android:layout_marginStart="10dp"
    android:padding="10dp"
    android:layout_marginEnd="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/book_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
        android:id="@+id/book_title_recycler"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Titlu"
        android:textSize="20dp"
        android:textStyle="bold"
        android:textColor="@color/gray"
        android:background="@color/fade_black"
        app:layout_constraintBottom_toTopOf="@id/book_authors_recycler"
        app:layout_constraintStart_toStartOf="@id/book_authors_recycler"/>


    <TextView
        android:id="@+id/book_authors_recycler"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Autori"
        android:layout_margin="10dp"
        app:layout_constraintBottom_toBottomOf="@id/book_image"
        app:layout_constraintStart_toStartOf="@id/book_image"/>

</androidx.constraintlayout.widget.ConstraintLayout>